SHOW TABLES;
-- SELECT * FROM actor;
-- SELECT 
-- 	last_name , 
--     first_name, 
--     actor_id, 
--     actor_id +1 AS add_one_in_id -- we can give name to the colomn otherwise it is displaying like (actor_id + 1) if we want string in name use "" or ''
-- FROM actor; -- order of result now last_name and first_name we can arthmatics operators in the select as well

SELECT city FROM city; -- first city is the column and last city is the table name
SELECT DISTINCT city FROM city; -- for this we get all the unique city 


